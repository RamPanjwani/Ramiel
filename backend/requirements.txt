# Ramiel Backend — Phase 0 Dependencies
# Minimal set for skeleton. Additional deps added per-phase.
#
# Approved libraries per Rules.md §2:
#   fastapi, uvicorn, pyyaml, structlog, psutil
#
# Dev tooling:
#   pytest, mypy, ruff

# --- Runtime ---
fastapi>=0.115.0
uvicorn>=0.30.0
pyyaml>=6.0
structlog>=24.0
psutil>=6.0
pydantic>=2.0

# --- Dev / Test ---
pytest>=8.0
mypy>=1.10
ruff>=0.5.0
httpx>=0.27.0          # for TestClient async testing
